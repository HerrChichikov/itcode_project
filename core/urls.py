from django.urls import path
from core import views

app_name = 'core'

urlpatterns = [

    # path('accounts/', views.AccountList.as_view(), name='accounts'),
    # path('account_detail/<int:pk>', views.AccountDetail.as_view(), name='account_detail'),
    # path('account_create', views.AccountCreate.as_view(), name='account_create'),
    # path('account_update/<int:pk>', views.AccountUpdate.as_view(), name='account_update'),
    # path('account_delete/<int:pk>', views.AccountDelete.as_view(), name='account_delete'),
    # path('', views.get_main_page, name='main'),
]
